KEY CASTRO WEBSITE v3.8.17 - ONE CMD AUTO DEPLOY

This package updates the local website, preserves private configuration, runs the full test suite,
stages only approved release files, creates a release commit when needed, pushes origin and
renderdeploy, triggers the existing Render service, and verifies the live site before reporting success.

This release changes only the Home transition after the three featured system cards. Explore All Systems →
is the primary action to /system-templates; See Services & Pricing → remains the secondary action to /services.
The approved CTA composition uses only CTA-scoped HTML/CSS plus direct test/release/live-verification dependencies.

Success is reported only after the live health endpoint reports v3.8.17; the Home page has exactly three
priced featured-system cards; both CTA destinations and the approved CTA markup/style hooks are live;
Services & Pricing remains intact; approved $160 existing-system pricing remains; maintenance remains
$39/month or $390/year; upgrades remain $79 / $149 / New System / Large Expansion — Price by Agreement;
the footer remains intact; and exactly three published systems remain live.
